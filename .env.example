ANTHROPIC_API_KEY=your-api-key-here

# Referral programme (optional — defaults shown)
REFERRER_REWARD=10
REFEREE_REWARD=5
REWARD_CURRENCY=GBP
# Used to build absolute share links shown to admins; falls back to the request host.
PUBLIC_BASE_URL=
